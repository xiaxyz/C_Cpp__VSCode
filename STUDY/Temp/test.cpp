#include "C:/VSCode__C_Cpp/STUDY/FIO.h"
#include <iostream>
int main()
{
    CFIO
// ShowWindow(GetConsoleWindow(),SW_HIDE);
// Mat img = imread("C:/Users/31381/Downloads/十三.png", IMREAD_UNCHANGED);
// namedWindow("十三英桀", WINDOW_FREERATIO);
// imshow("十三英桀", img);
// waitKey(0);
// destroyAllWindows()
    int a;
    std::cin >> a;
#ifdef XIAXDEFINE
    for (int i = 0; i < a; ++i)
    {
        std::cout << i << std::endl;
    }
#endif
    return 0;
}
